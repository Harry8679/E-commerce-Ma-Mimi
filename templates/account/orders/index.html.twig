{% extends 'base.html.twig' %}

{% block title %}Mes commandes{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto">
    <div class="mb-8">
        <h1 class="text-4xl font-bold text-gray-900 mb-2">Mes commandes</h1>
        <p class="text-gray-600">Suivez l'état de vos commandes et consultez votre historique</p>
    </div>

    {% if orders is empty %}
        {# Aucune commande #}
        <div class="bg-white rounded-lg shadow-md p-12 text-center">
            <div class="text-6xl mb-4">📦</div>
            <h2 class="text-2xl font-bold text-gray-900 mb-2">Aucune commande</h2>
            <p class="text-gray-600 mb-6">Vous n'avez pas encore passé de commande</p>
            <a href="{{ path('app_shop') }}" class="inline-block bg-amber-600 hover:bg-amber-700 text-white px-8 py-3 rounded-lg font-semibold transition">
                Découvrir nos produits
            </a>
        </div>
    {% else %}
        {# Liste des commandes #}
        <div class="space-y-4">
            {% for order in orders %}
                <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition">
                    <div class="p-6">
                        <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
                            {# Informations principales #}
                            <div class="flex-1">
                                <div class="flex items-center gap-3 mb-2">
                                    <h3 class="text-lg font-bold text-gray-900">
                                        Commande {{ order.orderNumber }}
                                    </h3>
                                    
                                    {# Badge statut #}
                                    <span class="px-3 py-1 rounded-full text-xs font-semibold
                                        {% if order.status == 'pending' %}bg-yellow-100 text-yellow-800
                                        {% elseif order.status == 'paid' %}bg-blue-100 text-blue-800
                                        {% elseif order.status == 'processing' %}bg-purple-100 text-purple-800
                                        {% elseif order.status == 'shipped' %}bg-indigo-100 text-indigo-800
                                        {% elseif order.status == 'delivered' %}bg-green-100 text-green-800
                                        {% elseif order.status == 'cancelled' %}bg-red-100 text-red-800
                                        {% else %}bg-gray-100 text-gray-800
                                        {% endif %}">
                                        {{ order.statusLabel }}
                                    </span>
                                </div>
                                
                                <div class="text-sm text-gray-600 space-y-1">
                                    <p>
                                        <span class="font-semibold">Passée le :</span>
                                        {{ order.createdAt|date('d/m/Y à H:i') }}
                                    </p>
                                    {% if order.paidAt %}
                                        <p>
                                            <span class="font-semibold">Payée le :</span>
                                            {{ order.paidAt|date('d/m/Y à H:i') }}
                                        </p>
                                    {% endif %}
                                    <p>
                                        <span class="font-semibold">{{ order.orderItems|length }} article(s)</span>
                                    </p>
                                </div>
                            </div>

                            {# Montant #}
                            <div class="text-center md:text-right">
                                <p class="text-sm text-gray-600 mb-1">Montant total</p>
                                <p class="text-2xl font-bold text-amber-700">
                                    {{ order.totalAmount|number_format(2, ',', ' ') }} €
                                </p>
                            </div>

                            {# Actions #}
                            <div class="flex md:flex-col gap-2">
                                <a href="{{ path('app_account_order_show', {id: order.id}) }}" 
                                   class="bg-amber-600 hover:bg-amber-700 text-white px-6 py-2 rounded-lg font-semibold transition text-center">
                                    Voir détails
                                </a>
                                
                                {% if order.status == 'delivered' %}
                                    <button class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-6 py-2 rounded-lg font-semibold transition">
                                        Télécharger facture
                                    </button>
                                {% endif %}
                            </div>
                        </div>

                        {# Aperçu des articles #}
                        <div class="mt-4 pt-4 border-t border-gray-200">
                            <div class="flex items-center gap-4 overflow-x-auto">
                                {% for item in order.orderItems|slice(0, 4) %}
                                    <div class="flex-shrink-0">
                                        <div class="w-16 h-16 bg-gray-100 rounded-lg overflow-hidden">
                                            {% if item.product and item.product.image %}
                                                <img src="/uploads/products/{{ item.product.image }}" 
                                                     alt="{{ item.productName }}" 
                                                     class="w-full h-full object-cover"
                                                     title="{{ item.productName }} (x{{ item.quantity }})">
                                            {% else %}
                                                <div class="w-full h-full flex items-center justify-center">
                                                    <span class="text-2xl">🥃</span>
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                {% endfor %}
                                
                                {% if order.orderItems|length > 4 %}
                                    <div class="flex-shrink-0 text-sm text-gray-600 font-semibold">
                                        +{{ order.orderItems|length - 4 }} autre(s)
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}