{% extends 'base.html.twig' %}

{% block title %}Accueil - Boutique de Rhums{% endblock %}

{% block body %}
<div class="text-center">
    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-amber-700 to-amber-900 text-white rounded-lg shadow-xl p-12 mb-12">
        <h1 class="text-5xl font-bold mb-4">Bienvenue dans notre boutique de Rhums</h1>
        <p class="text-xl mb-8">Découvrez notre sélection de rhums d'exception des Caraïbes et d'ailleurs</p>
        <a href="#" class="bg-white text-amber-800 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition">
            Découvrir nos rhums
        </a>
    </div>

    <!-- Features -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
        <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="text-4xl mb-4">🚚</div>
            <h3 class="text-xl font-bold mb-2">Livraison rapide</h3>
            <p class="text-gray-600">Expédition sous 24-48h partout en France</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="text-4xl mb-4">✅</div>
            <h3 class="text-xl font-bold mb-2">Paiement sécurisé</h3>
            <p class="text-gray-600">Transactions protégées par Stripe et PayPal</p>
        </div>
        <div class="bg-white p-6 rounded-lg shadow-md">
            <div class="text-4xl mb-4">⭐</div>
            <h3 class="text-xl font-bold mb-2">Sélection premium</h3>
            <p class="text-gray-600">Des rhums soigneusement sélectionnés</p>
        </div>
    </div>

    <!-- Call to Action -->
    <div class="bg-white rounded-lg shadow-md p-8">
        <h2 class="text-3xl font-bold mb-4">Prêt à commencer ?</h2>
        <p class="text-gray-600 mb-6">Créez votre compte dès maintenant et profitez de nos offres exclusives</p>
        {% if not app.user %}
            <a href="{{ path('app_register') }}" class="bg-amber-600 hover:bg-amber-700 text-white px-8 py-3 rounded-lg font-semibold inline-block">
                Créer mon compte
            </a>
        {% else %}
            <a href="#" class="bg-amber-600 hover:bg-amber-700 text-white px-8 py-3 rounded-lg font-semibold inline-block">
                Voir la boutique
            </a>
        {% endif %}
    </div>
</div>
{% endblock %}