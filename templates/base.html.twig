<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Boutique de Rhums{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🥃</text></svg>">
    
    {% block stylesheets %}
        {# Tailwind CSS via CDN #}
        <script src="https://cdn.tailwindcss.com"></script>
    {% endblock %}
</head>
<body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex">
                    <div class="flex-shrink-0 flex items-center">
                        <a href="{{ path('app_home') }}" class="text-2xl font-bold text-amber-800">
                            🥃 Dikukuli
                        </a>
                    </div>
                    <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                        <a href="{{ path('app_home') }}" class="{{ app.request.attributes.get('_route') == 'app_home' ? 'border-amber-500 text-amber-700' : 'border-transparent text-gray-900 hover:border-amber-500 hover:text-amber-700' }} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Accueil
                        </a>
                        <a href="{{ path('app_shop') }}" class="{{ app.request.attributes.get('_route') == 'app_shop' ? 'border-amber-500 text-amber-700' : 'border-transparent text-gray-900 hover:border-amber-500 hover:text-amber-700' }} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Boutique
                        </a>
                        <a href="{{ path('app_about') }}" class="{{ app.request.attributes.get('_route') == 'app_about' ? 'border-amber-500 text-amber-700' : 'border-transparent text-gray-900 hover:border-amber-500 hover:text-amber-700' }} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            À propos
                        </a>
                        <a href="{{ path('app_about') }}" class="{{ app.request.attributes.get('_route') == 'app_about' ? 'border-amber-500 text-amber-700' : 'border-transparent text-gray-900 hover:border-amber-500 hover:text-amber-700' }} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium">
                            Blog
                        </a>
                    </div>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:items-center space-x-4">
                    {% if app.user %}
                        <a href="#" class="text-gray-700 hover:text-amber-700">
                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
                            </svg>
                        </a>
                        <div class="relative ml-3">
                            <div class="flex items-center space-x-2">
                                👋 Bonjour&nbsp;<span class="text-gray-700 font-bold">{{ app.user.firstName }}</span>
                                <a href="{{ path('app_logout') }}" class="bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                                    Déconnexion
                                </a>
                            </div>
                        </div>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="text-gray-700 hover:text-amber-700 px-3 py-2 rounded-md text-sm font-medium">
                            Connexion
                        </a>
                        <a href="{{ path('app_register') }}" class="bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                            Inscription
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    <!-- Messages flash -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="mb-4 p-4 rounded-lg {% if label == 'success' %}bg-green-100 text-green-800{% elseif label == 'error' %}bg-red-100 text-red-800{% else %}bg-blue-100 text-blue-800{% endif %}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>

    <!-- Contenu principal -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="text-center">
                <p>&copy; {{ 'now'|date('Y') }} Rhum Shop. Tous droits réservés.</p>
                <p class="mt-2 text-sm text-gray-400">L'abus d'alcool est dangereux pour la santé. À consommer avec modération.</p>
            </div>
        </div>
    </footer>

    {% block javascripts %}
    {% endblock %}
</body>
</html>